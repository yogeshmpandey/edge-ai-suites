std_msgs/Header header

# Bounding box center coordinates:
uint16 cx
uint16 cy

# Bounding box dimensions:
uint16 width
uint16 height

# Bounding box angle:
float32 angle

# Unique string id of the object class:
string object_id

# AI detection confidence level:
float32 confidence_level
