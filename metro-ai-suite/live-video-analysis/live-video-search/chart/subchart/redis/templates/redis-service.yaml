apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    {{- include "lvs.redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    app: redis
