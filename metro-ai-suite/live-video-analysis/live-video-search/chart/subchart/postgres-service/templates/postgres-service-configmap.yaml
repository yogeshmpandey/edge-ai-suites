apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-service-config
  labels:
    {{- include "lvs.postgres.labels" . | nindent 4 }}
data:
  POSTGRES_DB: {{ .Values.global.env.postgresDb | quote }}
  PGDATA: "/var/lib/postgresql/data/pgdata"
