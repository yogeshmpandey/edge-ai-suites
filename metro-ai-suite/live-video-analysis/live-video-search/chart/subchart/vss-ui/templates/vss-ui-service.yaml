apiVersion: v1
kind: Service
metadata:
  name: vss-ui
  labels:
    {{- include "lvs.vssui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: vss-ui
