# --- Runtime + orchestrator ---
flask==3.0.3
pyyaml==6.0.2
requests==2.32.3
tqdm==4.66.5
psutil==6.0.0

# --- Runtime architecture note ---
# The heavy inference stack (Ultralytics + torch + OpenCV) is used by the
# host-side bootstrap flow (dataset fetch → REAL-Colon train → OpenVINO
# export). Runtime inference at deploy time happens inside the container.

# --- Bootstrap-only: training + export (Ultralytics + OpenVINO) ---
# torch + torchvision are installed by `make backend-venv` from the
# Intel XPU wheel index BEFORE this requirements.txt, because they are not
# available on PyPI:
#   pip install torch==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/xpu
ultralytics==8.4.75
openvino==2026.2.0
opencv-python==4.10.0.84
numpy==2.1.3
pillow==10.4.0
